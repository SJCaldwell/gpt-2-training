{
    "project_name": "traingpt-test",
    "experiment_name": "memory_test",
    "learning_rate": 3e-4,
    "batch_size": 16,
    "grad_accum_steps": 1,
    "max_steps": 10000,
    "warmup_steps": 1000,
    "eval_every": 500,
    "save_every": 1000,
    "optimizer": "adamw",
    "weight_decay": 0.1,
    "grad_clip": 1.0,
    "train_path": "data/processed/train.pt",
    "mixed_precision": true,
    "seed": 42
}